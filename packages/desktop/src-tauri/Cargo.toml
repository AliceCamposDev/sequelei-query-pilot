[package]
name = "sequelei-desktop"
version = "0.1.0"
edition = "2021"

[lib]
name = "sequelei_desktop"
path = "src/lib.rs"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
